kind = "web"
previewPath = "/"
title = "Orbs Academy"
version = "1.0.0"
id = "artifacts/orbs-academy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24743

[services.development]
run = "pnpm --filter @workspace/orbs-academy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/orbs-academy", "run", "build" ]
publicDir = "artifacts/orbs-academy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24743"
BASE_PATH = "/"
